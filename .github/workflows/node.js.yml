name: Publish a package
on:
  pull_request:
jobs:
  run-build-test-lint-publish-package:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v1
      - name: Get PR Number
        run: |
          echo ${{ github.event.number }}
            
      # Find the PR associated with this push, if there is one.
      - uses: jwalton/gh-find-current-pr@v1
        id: findPr
        
      # This will echo "Your PR is 7", or be skipped if there is no current PR.
      - run: echo ${{ steps.findPr.outputs }}
      
      - uses: actions/github-script@v4
        id: set-result
        with:
          script: console.log(${{ steps.findPr.outputs }})
          result-encoding: string
      - name: Get result
        run: echo "${{steps.set-result.outputs.result}}"
    
#       - name: Create Release
#         uses: actions/create-release@v1
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
#         with:
#           tag_name: ${{ github.event.number }} # Get version from package.json
#           release_name: Release ${{ steps.findPr.outputs }}
#           body: | # Changes or features in the corresponding release
#             Package initial release 
#           draft: false
#           prerelease: false # 
